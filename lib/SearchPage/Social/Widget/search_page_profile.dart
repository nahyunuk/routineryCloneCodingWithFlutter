import 'package:flutter/material.dart';
class SearchPageProfile extends StatelessWidget {
  const SearchPageProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
